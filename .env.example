# X (Twitter) API Credentials
# Get these from https://developer.twitter.com/en/portal/dashboard

# Your X App's Client ID (public)
X_CLIENT_ID=your_client_id_here

# Your X App's Client Secret (keep this secret!)
X_CLIENT_SECRET=your_client_secret_here

# OAuth callback URL (update with your domain)
# For local development: http://localhost:3000/api/auth/callback
# For production: https://your-app.vercel.app/api/auth/callback
X_REDIRECT_URI=http://localhost:3000/api/auth/callback
